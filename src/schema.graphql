type Query {
    info: String!
    feed: [Link!]!
    getLink(id: ID!): Link
}


type Mutation {
    postLink(url: String!, description: String!): Link!
    updateLink(id: ID!, url: String, description: String): Link
}

type Link {
    id: ID!
    description: String!
    url: String!
}